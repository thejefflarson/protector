{
  "name": "protector-dashboard-web",
  "private": true,
  "description": "Vendored, self-hosted graph renderer for the protector dashboard (no third-party CDN). Dependabot tracks these versions; rebuild the bundle with `npm run build`.",
  "type": "module",
  "scripts": { "build": "node build.mjs" },
  "dependencies": { "beautiful-mermaid": "1.1.3" },
  "devDependencies": { "esbuild-wasm": "0.24.0" }
}
